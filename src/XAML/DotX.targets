<?xml version="1.0" encoding="utf-8"?>
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <UsingTask TaskName="ProcessXamlTask" 
             AssemblyFile="/home/flavius/Programming/dotnet/DotX/src/XAML/DotX.Xaml.MsBuild/bin/Debug/net5.0/DotX.Xaml.MsBuild.dll"/>

  <ItemGroup>
    <!-- Include definitions from dxc.xml, which defines the DXCShader item. -->
    <PropertyPageSchema Include="$(MSBuildThisFileDirectory)XAML.xml" />
    <!-- Hook up DXCShader items to be built by the DXC target. -->
    <AvailableItemName Include="XAMLDoc">
      <Targets>XAML</Targets>
    </AvailableItemName>
  </ItemGroup>

  <PropertyGroup>
    <CoreCompileDependsOn>XAML;$(CoreCompileDependsOn)</CoreCompileDependsOn>
  </PropertyGroup>

  <Target Name="XAML"
          Returns="@(XAML->'%(Filename).g.cs')">
    <PropertyGroup>
      <NewCSFiles/>
    </PropertyGroup>
    
    <ProcessXamlTask CurrentProject="$(MSBuildProjectFullPath)"
                     InputFiles="@(XAML)">
      <Output TaskParameter="FilesToAdd" PropertyName="NewCSFiles"/>
    </ProcessXamlTask>

    <ItemGroup>
      <Compile Include="$(NewCSFiles)"/>
    </ItemGroup>
  </Target>

  <ItemGroup>
    <XAML Include="**/*.xaml"/>
  </ItemGroup>
</Project>